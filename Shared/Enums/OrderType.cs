namespace PBL3.Shared.Enums // Định nghĩa namespace PBL3.Shared.Enums quản lý cấu trúc code truyền tải và validator.
{
    public enum OrderType : byte // Định nghĩa kiểu liệt kê (enum) OrderType kiểu byte đại diện cho các trạng thái trong hệ thống.
    {
        Online = 0, // Giá trị liệt kê 'Online' có giá trị nguyên là 0.
        POS = 1 // Giá trị liệt kê 'POS' có giá trị nguyên là 1.
    }
}
